/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Change your site title here
  env: {
    title: 'Balance On Life',
    titleDescription: '',
  },
}

module.exports = nextConfig
